;-----------------------------------------------------------------------------
; SERIAL.MAC
;-----------------------------------------------------------------------------
.IF !.DEFINED(PS2_POINTER)	;DON'T DEFINE MACROS IF THEY EXIST ALREADY

.IMPORT XMSERIAL_INIT, PS2_GETKEY

.ENDIF
