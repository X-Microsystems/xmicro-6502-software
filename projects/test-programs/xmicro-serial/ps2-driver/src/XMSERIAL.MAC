;-----------------------------------------------------------------------------
; SERIAL.MAC
;-----------------------------------------------------------------------------
.IF !.DEFINED(PS2_POINTER)	;DON'T DEFINE MACROS IF THEY EXIST ALREADY

.IMPORTZP PS2_POINTER
.IMPORT XMSERIAL_INIT, PS2_KEYBUFFER, PS2_KEYINDEX

.ENDIF
